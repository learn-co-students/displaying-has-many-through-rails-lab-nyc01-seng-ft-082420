<h1> <%= @doctor.name %></h1>
<h1> <%= @doctor.department %></h1>


<% @doctor.appointments.each do |appt| %>
    <%= link_to "#{appt.appointment_datetime.strftime("%B %e, %Y at %H:%M")} with #{appt.patient.name}", patient_path(appt.patient) %> <br>
  <% end %>