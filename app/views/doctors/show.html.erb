<h1><%= @doctor.name %></h1>
<p><%= @doctor.department %></p>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>DateTime: <%= appt.date_time_formatted %> - Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
	<% end%>
</ul>