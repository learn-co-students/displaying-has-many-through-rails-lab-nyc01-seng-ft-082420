<%= @doctor.name %><br>
<%= @doctor.department %><br>
<h3>Appintments</h3>
<% @doctor.appointments.each do |appointment| %>
<%= appointment.appointment_datetime.strftime('%B %d, %Y at %k:%M') %> <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
<% end %>
